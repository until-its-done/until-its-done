import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'until-its-done',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
